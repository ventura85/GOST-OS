#!/bin/bash
set -euo pipefail
# Uruchom live-build w minimalnym środowisku, aby uniknąć rekurencji i przepełnienia listy argumentów
env -i HOME="$HOME" PATH="/usr/sbin:/usr/bin:/bin:/sbin" lb build --force 2>&1 | tee build.log
